"""Agente de búsqueda para Emergency Control.

El diseño formal que implementa este paquete está en `project/design.md`.

Módulos:
    domain  — constantes del escenario (Σ) indexadas + cierre de dependencias
    state   — estado canónico, estado inicial y prueba de meta
    actions — Applicable(s) y Result(s, a)
"""
